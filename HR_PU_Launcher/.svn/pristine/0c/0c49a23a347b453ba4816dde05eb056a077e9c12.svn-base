<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background" android:drawable="@color/progressbar_back"/>
    <item android:id="@android:id/progress">
        <clip>
            <color android:color="@color/color_red" />
        </clip>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <color android:color="@color/color_red" />
        </clip>
    </item>

</layer-list>